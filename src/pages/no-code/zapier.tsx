import Layout from "@/components/Layout";
import SecondHero from "@/components/reused/SecondHero";

export default function Zapier(){
    return (
        <Layout>
            <SecondHero heading="Automation with Zapier" description="Take control of HTML, CSS, and JavaScript in a visual canvas. Webflow generates clean, semantic code that’s ready to publish." />
        </Layout>
    )
} 